#include "main.h"
/**
 * print_alphabet - It checks description
 * Description: It prints the alphabets in lowercase and new line follows
 * Return: Nothing
 */
void print_alphabet(void)
{
	char a;
	
	for (a = 'a'; a <= 'z'; a++)
		_putchar(a);
	_putchar('\n');
}
